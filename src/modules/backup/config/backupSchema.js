// Версія ФОРМАТУ конверта бекапу. Інкрементувати лише при зміні структури
// envelope (не при додаванні таблиць — перелік таблиць визначається динамічно
// з db.tables у BackupService).
export const BACKUP_FORMAT_VERSION = 1

// Таблиці, які свідомо НЕ потрапляють у бекап (напр. майбутні кеш/ephemeral).
// Поки порожньо — бекапиться вся схема.
export const BACKUP_EXCLUDE = []
